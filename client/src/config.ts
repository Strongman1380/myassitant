// API URL - empty string means same origin (works for both dev and production)
// In dev: Vite proxies /api/* to the Vercel functions
// In production: API routes are at /api/* on the same domain
export const API_URL = '';
